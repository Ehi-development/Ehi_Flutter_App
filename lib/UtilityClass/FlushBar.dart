/*
import 'package:flushbar/flushbar.dart';
import 'package:flutter/material.dart';
import 'package:hey_flutter/UtilityClass/Theme.dart';

void showFlushbar({BuildContext context,title,icon,message,color=Colors.redAccent}) {
  Flushbar(
    margin: EdgeInsets.all(8),
    borderRadius: MoobTheme.radius,
    backgroundColor: MoobTheme.textColor,
    boxShadows: [
      BoxShadow(
        color: Colors.black.withOpacity(0.2),
        offset: Offset(3, 3),
        blurRadius: 3,
      ),
    ],
    title: title,
    message: message,
    icon: Icon(
      icon,
      size: 28,
      color: color,
    ),
    duration: Duration(seconds: 2),
  )..show(context);
}
 */